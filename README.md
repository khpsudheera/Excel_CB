# Excel_CB
## 🧼 Data Cleaning Using Advanced Excel

1. Use **Ctrl + F** → Replace missing data with `"Not Available"` when no value exists.  
2. Use the **TRIM()** function to remove extra spaces from text entries.  
   - Example: `=TRIM(@Studio)` (place your column name inside the brackets).  
3. Use **Ctrl + Alt + V** to open **Paste Special** and paste values/formats as needed.  
4. Apply **Conditional Formatting** to:  
   - Highlight duplicate values  
   - Highlight values greater than or less than a specified number  
5. Use **Remove Duplicates** under the Data tab to eliminate repeated records.  
6. Use **Text to Columns → Delimited** to split combined text into separate columns.  
